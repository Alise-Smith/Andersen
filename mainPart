<body>
  <script>
	  var changeFamily=JSON.parse(JSON.stringify(family));
    function tree(family){
		var li=document.createElement("li");
		li.innerHTML=family.name; 
		var ul=document.createElement("ul");
		if(family.children.length!=0) 
			for(i=0;i<family.children.length;i++)
				for (var key in family) 
					if (typeof family[key] == 'object')
						ul.appendChild(tree(family.children[i]));
			li.appendChild(ul);
		return li;
	}
  ul=document.createElement("ul");
  ul.appendChild(tree(changeFamily));
  document.getElementsByTagName("body")[0].appendChild(ul);
  </script>
</body>
